import 'package:flutter/material.dart';

class GoldPriceModel extends StatelessWidget {
  const GoldPriceModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
